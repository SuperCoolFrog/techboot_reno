seed(2,2, gate_or). 

rows(5).
cols(5).

cell_state(out_of_bounds).
cell_state(super_position).
cell_state(empty).
cell_state(gate_and).
cell_state(gate_or).
cell_state(current_vertical).
cell_state(current_horizontal).

% mod X, // Y
% setup(TotalCells, Rows, Cols, Idx, [Out|Rest]) :-
    
    
    
